### 6.4. `Config.toml` — конфигурация приложения

```toml
port = 3000
database_url = "postgres://ytk_user:password@localhost:5432/ytk_archive"
max_bot_token = "your_bot_token_here"
file_storage_path = "./uploads"